NODE_ENV=development
PORT=3000

# REQUIRED by NWS: identify your application (include contact info)
NWS_USER_AGENT="weather-service (you@example.com)"

# Optional: allow only your front-end origins (comma-separated). If empty, all origins are blocked.
CORS_ALLOWLIST="http://localhost:3000,http://localhost:5173,http://localhost:3001"

# Minimal Basic Auth (store ONLY the bcrypt hash in the service)
AUTH_USERNAME="demo"
# bcrypt hash for password
AUTH_PASSWORD_HASH="$2b$12$0V6XFpeJeWjAKF6vDyh2Pe8p5hyfvKLXHxvstOG/FHkH9HeRS6Ag."
